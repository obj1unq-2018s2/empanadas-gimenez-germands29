import empleados.*
import testEmpleados.*
	
	test "baigorria gasta otros 8000, deuda pasa a 12000, total dinero sigue 0"{
		baigorria.gastar(8000)
		assert.equals(12000,baigorria.totalDeuda())
	}